# Own, prefixed env vars -- kept separate from the host repo's vars even
# though they point at the same Supabase project for now. See README.md.

DEKAZ_DATABASE_URL=
DEKAZ_SUPABASE_URL=
DEKAZ_SUPABASE_SERVICE_ROLE_KEY=
DEKAZ_OPENAI_API_KEY=
DEKAZ_OPENAI_MODEL=gpt-4o-mini
DEKAZ_AI_MOCK_MODE=false
